# =================================================================
# Richie's Game Collection - Docker Configuration
# =================================================================
# Copy this file to .env and customize the values as needed
# All variables have sensible defaults if not specified

# =================================================================
# PROJECT SETTINGS
# =================================================================

# Project name (used for container naming and Traefik routing)
COMPOSE_PROJECT_NAME=minis

# Domain name for external access
DOMAIN=minis.richie.ch

# Additional domain aliases (all load balanced across both containers)
DOMAIN_ALIAS_1=eiger.software
DOMAIN_ALIAS_2=www.eiger.software

# Test domains for direct container access (bypasses load balancing)
# These allow you to test each container individually
TEST_DOMAIN_1=test1.minis.richie.ch
TEST_DOMAIN_2=test2.minis.richie.ch

# Examples for different scenarios:
# Production domains: DOMAIN=minis.richie.ch, DOMAIN_ALIAS_1=eiger.software
# Test domains: TEST_DOMAIN_1=test1.minis.richie.ch, TEST_DOMAIN_2=test2.minis.richie.ch
# Custom domains: DOMAIN=games.example.com, DOMAIN_ALIAS_1=play.example.com

# =================================================================
# CONTENT DIRECTORIES
# =================================================================

# Content directory for container 1 (www or www-red)
CONTENT_DIR_1=www

# Content directory for container 2 (www or www-red)
CONTENT_DIR_2=www

# Examples for different scenarios:
# Both serving production: CONTENT_DIR_1=www, CONTENT_DIR_2=www
# One testing: CONTENT_DIR_1=www, CONTENT_DIR_2=www-red
# Both testing: CONTENT_DIR_1=www-red, CONTENT_DIR_2=www-red

# =================================================================
# RESOURCE LIMITS
# =================================================================

# CPU limits per container (0.1 = 10% of one CPU core)
CPU_LIMIT=0.5
CPU_RESERVATION=0.1

# Memory limits per container
MEMORY_LIMIT=256M
MEMORY_RESERVATION=64M

# =================================================================
# NGINX PERFORMANCE
# =================================================================

# Number of nginx worker processes (auto = detect CPU cores)
NGINX_WORKER_PROCESSES=auto

# Worker connections per process
NGINX_WORKER_CONNECTIONS=1024

# =================================================================
# SECURITY & RATE LIMITING
# =================================================================

# Rate limiting for main content (requests per second)
RATE_LIMIT_REQUESTS=10r/s

# Burst size for rate limiting (how many requests can exceed rate temporarily)
RATE_LIMIT_BURST=20

# =================================================================
# LOGGING CONFIGURATION
# =================================================================

# Log rotation settings
LOG_ROTATE_SIZE=10M
LOG_ROTATE_COUNT=20

# Examples:
# LOG_ROTATE_SIZE=50M (rotate when logs reach 50MB)
# LOG_ROTATE_COUNT=30 (keep 30 old log files)

# =================================================================
# NETWORK SETTINGS
# =================================================================

# Ports exposed to Traefik network (configurable as requested)
# These ports are exposed on the host for direct access if needed
# Container 1 will be accessible at localhost:INTERNAL_PORT_1
# Container 2 will be accessible at localhost:INTERNAL_PORT_2
INTERNAL_PORT_1=11888
INTERNAL_PORT_2=11889

# Examples:
# Same ports: INTERNAL_PORT_1=11888, INTERNAL_PORT_2=11888 (not recommended, causes conflicts)
# Different ports: INTERNAL_PORT_1=11888, INTERNAL_PORT_2=11889 (recommended)
# Custom ports: INTERNAL_PORT_1=8080, INTERNAL_PORT_2=8081

# =================================================================
# DEVELOPMENT SETTINGS
# =================================================================

# Set to 'true' to enable debug logging (more verbose logs)
DEBUG_LOGGING=false

# Timezone for log timestamps (e.g., Europe/Zurich, America/New_York, UTC)
TIMEZONE=Europe/Zurich

# =================================================================
# TRAEFIK INTEGRATION
# =================================================================

# The external Traefik network name (should match your Traefik setup)
TRAEFIK_NETWORK=traefik_public

# Certificate resolver name (should match your Traefik configuration)
CERT_RESOLVER=myresolver

# =================================================================
# MAINTENANCE
# =================================================================

# Set to 'true' to enable maintenance mode (shows maintenance page)
MAINTENANCE_MODE=false
